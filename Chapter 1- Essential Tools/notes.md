### What is Linux ?
#### Linux is a free opensource operating system that manages the computer's resources and provides an environment to run applications.
---
### What is Shell ?
#### Shell is the program inside the terminal that reads and executes the commands.
---
### What is Command ?
#### A command is a instruction that performs specific task.
- #### `Internal Command` : *Internal command is a command that executes from shell it doesnot executes from file that stores in computer's memory*
- #### `External Command` : *An External Command is a command that executes from copumter's disk or memory*
---
### What is $PATH ?
#### A "$PATH" is environment variable that tells the shell that where it looks for execution.
---
### What is Pipe (|) ?
#### A Pipe will takes the first. command's output as input for second command