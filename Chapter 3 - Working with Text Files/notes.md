### `etc/passwd` :
- It is a system file that stores information about all user accounts on the Linux system.
- It doesnot store passwords anymore. Modern Linux stores encrypted passwords in `etc/shadow`.
- It is a plain text file that exist on almost every Linux system. Thats why books are used this to explain about text-processing commands.